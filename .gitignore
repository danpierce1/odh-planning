# Export directories (generated files, don't commit)
exports/

# macOS
.DS_Store

# Temporary files
*.tmp
*.swp
*~

# below follows advice that speckit init command mentions: Consider adding .claude/ (or parts of it) to .gitignore to prevent accidental credential leakage. 
.claude